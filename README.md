FoxOps CLI is currently scoped as an audit-only command-line toolkit.

Initial checks are planned for:

- Network reachability from the local runner
- TCP host:port validation
- Windows local account and password-policy inspection
- Structured JSON output for repeatable audit logs

FoxOps does not perform remediation, exploit testing, credential harvesting, firewall changes, or automatic system modification.
